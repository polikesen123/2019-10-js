1. 先获取数据， 封装成一个函数，去完成获取数据的操作
2. 渲染数据  
   1. 把从后台获取到的数据展示到页面上 按照列来展示
   2. 循环后台给的数据 然后把每一条数据拼接好添加到长度最小的那一列
   3. 这里封装了一个获取长度最小列的方法getMinLi
   4. 把元素集合转成数组 然后按照clientHeight进行排序由此找到最低的那个li
3. 滚动加载更多，长度最小的那个li露出来的时候就去加载新数据，loadMore方法

```
    function offset(ele){
        let t = ele.offsetTop
        let l = ele.offsetLeft
        let temp = ele.offsetParent
        while(temp){
            t+=temp.offsetTop+temp.clientTop
            l+=temp.offsetLeft+temp.clientLeft
            temp = temp.offsetParent
        }
        return {
            l,t
        }
    }
```

### 表格td的上级参照物是table并不是自己设置的那一个